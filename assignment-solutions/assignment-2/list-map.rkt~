#lang racket

(define (list-map f list)
  (define (reduce list result)
    (if (null? list)
        (reverse result)
        (reduce (cdr list) (cons (f (car list)) result))))
  (reduce list '()))

(list-map add1 '(1 2 3 4))
(list-map boolean? '(1 #t #f 4))